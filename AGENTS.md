# QtMaster工作约定

- 默认使用简体中文交流。开始任务先确认主机、用户、当前目录和Git项目根目录。
- 当前只维护市场条件化LLA模型；不要自动添加历史模型、对比方法或旧实验结果。
- 当前公共入口为 `qtmaster.build_model` 和 `python -m qtmaster.train`。
- 修改前检查Git状态和相关diff，保留用户已有修改；不覆盖或删除已有实验产物。
- 数据、预测、checkpoint和运行日志不进入Git；项目输出保存在本项目 `outputs/`。
- 凭据、私钥、个人绝对路径和机器本地配置不得提交；环境由当前机器独立配置。
- 数据须按 `data/manifest.json` 校验，只有来源可信时才能启用 `--trust-pickle`。
- 未获明确授权，不修改数据划分、标签规则、评估定义或回测口径。
- 当前配置为CSI300学习率0.001、CSI800-Direct学习率0.0005；所有模块共享一个Adam参数组。
- 正式实验默认种子0–4。正式长任务先检查资源，记录命令、环境、PID、日志、开始/结束时间及退出码。
- 启动长任务前确保断开客户端后仍可继续；不得干扰其他项目或其他用户的进程。
- 未请求运行实验时，不因修改代码或说明而自动启动正式训练。
- 检查命令为 `python -m unittest discover -s tests -v`；合成smoke不代表真实实验结果。
- 日常直接在 `main` 开发并推送；未经明确要求不创建其他分支、不强推或删除远程分支。
