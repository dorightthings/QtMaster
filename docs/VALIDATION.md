# 初始版本交付检查

检查日期：2026-09-06。这里记录代码移植检查，不把合成数据或历史预测的重放当作新选股结果。

## 已执行

- 模型：CSI300 / CSI800 两种配置，各 seed 0–4，共 10 组，与原正式主线的
  `state_dict`、前向、全部参数梯度及构造后的 RNG 状态逐元素一致，参数量 206,017。
- 数据：真实 CSI300 验证集 17,700 条标准化标签与原实现一致；同一组原始标签的
  训练截尾保留位置及标准化值一致；抽查的 6 个实际历史窗口与原采样器一致。
- 数据清单：六份 pickle 的清单字段与原数据契约对应，原文件大小核对通过；
  真正加载前，程序还会逐文件校验 SHA-256。
- 自动测试：22 项 `unittest` 通过，覆盖模型、数据、导出、指标和训练入口。
- 独立安装：editable 安装及 wheel 安装均能导入模型；wheel 从独立虚拟环境的
  `site-packages` 运行，不依赖旧服务器工程。
- 训练入口：两轮 CPU 合成训练完成，验证选择 checkpoint、保存预测及状态记录正常；
  合成检查明确不产生金融 IC/AR 指标。
- 回测接口：重放原 CSI300 seed 0 的桥接日与测试预测，完整覆盖 611 个交易日，
  无成本超额 AR 为 0.3002540605961333，IR 为 2.7618210526403457；
  与原结果一致（IR 差异低于 3e-15）。这验证回测接口，不是重新训练结果。

检查环境的关键版本为 Python 3.8、PyTorch 2.1.2、NumPy 1.23.5、pandas 1.5.3、
SciPy 1.10.1、pyqlib 0.9.7。README 提供 Python 3.10 的新环境创建方案；
未在交付时重新安装并验证所有操作系统、驱动和依赖组合。

## 使用边界

模型和核心训练协议保持，标准 DataLoader 替代原服务器的专用缓存调度、手工 RNG
消费及 GPU/CPU 进程交接包装。两台机器的完整训练结果不要求逐位一致。

数据链接尚待所有者补充，真实数据和 checkpoint 不包含在 Git 中。
本次没有重新运行双股票池五种子的完整训练，也没有把历史开发测试声明为新的独立验证。
